receivers:
  otlp:
    protocols:
      http:
        endpoint: "0.0.0.0:4318"

processors:
  batch:

exporters:
  otlphttp/supabase_logs:
    endpoint: https://oqrqdofbtytcihfttlau.supabase.co/functions/v1/otel
    compression: none
    encoding: json        # ★ FORCE JSON
    headers:
      apikey: "${SUPABASE_SERVICE_ROLE_KEY}"

service:
  pipelines:
    logs:
      receivers: [otlp]
      processors: [batch]
      exporters: [otlphttp/supabase_logs]
